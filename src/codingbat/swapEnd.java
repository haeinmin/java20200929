package codingbat;

public int[] swapEnds(int[] nums) {
	  
	int[] swapEnds = new int[nums.length]; {
	  
	  for (i=1; i<nums.length-1; i++) {
	    temp[i] += nums[i];
	    return swapEnds = nums[nums.length-1] + temp[i] + nums[0];
	  }
	  
}
}
